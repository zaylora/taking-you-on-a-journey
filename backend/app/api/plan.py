"""占位：/api/plan/refine（M5 局部重排 / 攻略导出）。

router 已建但暂不挂任何 endpoint，main.py include 后不影响 M1 验收路径。
"""
from fastapi import APIRouter

router = APIRouter()

# TODO(M5): POST /api/plan/refine —— 局部重排、攻略导出
